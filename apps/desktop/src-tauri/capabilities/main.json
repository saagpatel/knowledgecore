{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-local-vault",
  "description": "Bind the primary local desktop window to the app command surface without granting core plugin APIs or remote web content access.",
  "windows": ["main"],
  "permissions": []
}
